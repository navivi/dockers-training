apiVersion: v1
kind: Service
metadata:
  name: {{ include "dockers.fullname" . }}
  labels:
    {{- include "dockers.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "dockers.selectorLabels" . | nindent 4 }}
