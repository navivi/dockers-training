apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "dockers.fullname" . }}-test-connection"
  labels:
    {{- include "dockers.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "dockers.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
